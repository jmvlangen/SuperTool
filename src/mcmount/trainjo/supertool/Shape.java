package mcmount.trainjo.supertool;

//all different types of shapes supported by the plugin (used by the build tool)
public enum Shape {
	//Shape of a cube:
	CUBE,
	
	//Shape of the largest circle (actually a ball) fitting inside a cube:
	CIRCLE,
	
	//Shape of the largest cilinder that fits in a cube:
	CILINDER,
}
